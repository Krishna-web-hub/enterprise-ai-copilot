apiVersion: v1
kind: Service
metadata:
  name: copilot-backend
  namespace: ai-copilot
  labels:
    app: copilot-backend
spec:
  type: ClusterIP
  selector:
    app: copilot-backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
